services:
  core:
    image: ghcr.io/projekt-inzynierski-pri-kkwj/core:pri-2.0-final-release
    env_file:
      - config.env
    ports:
      - "8080:8080"
      - "5432:5432"
    environment:
      SPRING_PROFILES_ACTIVE: docker-prod
    restart: always

  ui:
    image: ghcr.io/projekt-inzynierski-pri-kkwj/ui:pri-2.0-final-release
    env_file:
      - config.env
    ports:
      - "80:80"
    restart: always
